#include<stdio.h>
#include<conio.h>
int main()
{
int num;
scanf("%d",&num);
if(num>0)
printf("number is positive");
else if(num<0)
printf("Number is negative");
else
printf("it is zero");
return 0;
}
